import React from "react";
import { MainLayout } from "../../components/layout/MainLayout";

const CartPage: React.FC = () => {
  return (
    <MainLayout>
      <div>Your cart is empty (demo)</div>
    </MainLayout>
  );
};

export default CartPage;
